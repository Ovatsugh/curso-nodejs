  <h1>Usuários Cadastrados:</h1>

    <div class="container">
        <div class="card-container">
            {{#each users}}
            <div class="card">
                <p>{{this.name}}</p>
            </div>
            {{/each}}
        </div>
    </div>